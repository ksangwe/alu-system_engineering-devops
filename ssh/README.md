# SSH

Scripts for SSH key generation and remote server connection.

## Files

- `0-use_a_private_key` — connects to a server using the private key `~/.ssh/school`
- `1-create_ssh_key_pair` — creates a 4096-bit RSA key pair named `school`
